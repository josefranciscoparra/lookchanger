const fs = require('fs')
const path = require('path')
console.log('[db:prep] Copia y pega el SQL de supabase.sql en tu consola de Supabase (SQL Editor).')
console.log('[db:prep] Archivo:', path.resolve(__dirname, '..', 'supabase.sql'))